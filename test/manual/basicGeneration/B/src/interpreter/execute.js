define(["parser", "internalB"], function(parser, internalB) {
    //independat code code code
    function exe(){
        //independat code code code
        parser();
        //independat code code code
    }
    //independat code code code

    internalB.execute = exe;

    return exe;
});